language: go
sudo: false

os:
  - linux
  - osx

before_install:
  - curl https://ready-steady.github.io/travis/install_gfortran.sh | bash

install:
  - go get -d -t ./...
  - make install
  - go test -v

notifications:
  email: false

addons:
  apt:
    packages:
      - gfortran
